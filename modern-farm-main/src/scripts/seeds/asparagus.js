/* Define a function for each seed type like "createCorn". 
Each function should return an object with the properties 'type', 'height' and 'output'. */

export const createAsparagus = () => {
    const seed = { 
        type: "Asparagus",
        height: 24,
        output: 4
    }
    return seed
}
